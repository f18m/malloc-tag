{
    "comment": "This configuration is used to aggregate together the results from the application threads produced by the 'multithread' example",
    "rule0": {
        "aggregate_trees": {
            "matching_regex": "ExampleThr/[0-9]*"
        }
    },
    "rule1": {
        "aggregate_trees": {
            "matching_regex": "YetAnThr/[0-9]*"
        }
    }
}